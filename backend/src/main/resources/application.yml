spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/erp
    username: erp
    password: erp
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
    locations: classpath:db/migration

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info

jwt:
  secret: votre_super_cle_secrete_a_changer_en_production_au_moins_512_bits
  expiration: 86400000